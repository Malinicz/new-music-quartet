import * as styledComponents from 'styled-components';
import styledTheme from './theme';

const {
  default: styled,
  css,
  injectGlobal,
  keyframes,
  ThemeProvider,
  ServerStyleSheet,
} = styledComponents;

export {
  css,
  injectGlobal,
  keyframes,
  ThemeProvider,
  ServerStyleSheet,
  styledTheme,
};

export default styled;
